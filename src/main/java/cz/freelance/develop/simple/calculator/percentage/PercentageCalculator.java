package cz.freelance.develop.simple.calculator.percentage;

public interface PercentageCalculator {

    double calculateNumber(double percentage, double totalNumber);
    double calculatePercentage(double number, double totalNumber);

}
